{
  "name": "github-repo-watcher",
  "version": "1.0.0",
  "description": "Monitor GitHub repo updates and send Telegram notifications via Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
